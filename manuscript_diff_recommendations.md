# Manuscript diff recommendations for draft v0.4

対象:`substantive_transparency_PSRM_draft_v04.md`。草稿本体は編集していない。以下は、今回実装したレプリケーションコード(`transparency_sim`:図1再生成・合成生成器・A0 基準測定器・自動テスト一式)と本文の整合を取るための提案である。各項目は場所・diff・理由の順。

---

## 1. 図1関連

### 1.1 Location: §5.6 結果(スロット)、図1の箇条書き

```diff
- **図1(理論・作成済み、v0.4 で改訂)。** 命題3の二側境界。(a) Pr(m = 0) の厳密値と上下界(q = 150, r = 5)。(b) 必要予算の三本の線形線——普遍下界 (q−r+1)(1−α^{1/r})、完全構造 B*(δ; q, 1) ≤ q(1−α^{1/r})、無構造 B*(δ; q, 0) = q(1−α)(仮定4の線形損失)——の ρ に対するスケーリング(r = 5, α = 0.05)。ファイル:fig1_budget_bounds.pdf。
+ **図1(理論・レプリケーションコードから再生成)。** 命題3の二側境界。(a) Pr(m = 0) の厳密値と上下界(q = 150, r = 5)。(b) 必要予算の三本の線形線——普遍下界 (q−r+1)(1−α^{1/r})、完全構造 B*(δ; q, 1) ≤ q(1−α^{1/r})、無構造 B*(δ; q, 0) = q(1−α)(仮定4の線形損失)——の ρ に対するスケーリング(r = 5, α = 0.05)。ファイル:transparency_sim/outputs/figures/fig1_budget_bounds.pdf(scripts/regenerate_fig1.py により決定的に再生成可能)。
```

Rationale: 図の出所を再現可能なスクリプトに固定する。パネル(b)の c = 0 線が q(1−α) であること、全回収停止時刻を比較量として使っていないことは、生成コードとテスト(`test_fig1b_c0_line_is_q_times_one_minus_alpha`)で機械的に保証済み。

### 1.2 Location: 編集方針「v0.3 からの主要改訂」(A4)および §4.5

確認結果:整合。変更不要。本文の c = 0 比較量は B*(δ; q, 0) = q(1−α)(§4.5 は ⌈q(1−α)⌉)で、実装と一致する。図は実数値の境界線を描いており、整数丸めの差は視認不能なので、キャプションへの追記は不要と判断する。

### 1.3 Location: 図1キャプション(投稿版で新設)

現行草稿は §5.6 の箇条書きのみで独立の図キャプションを持たないため、投稿版のキャプション文案を追加提案する。

```diff
+ 図1 種取得失敗確率と必要予算の二側境界。(a) Pr(m = 0) の厳密値 C(q−r, B)/C(q, B) と、命題3の有限標本上下界(q = 150, r = 5)。(b) 希釈率 ρ = q/r に対する必要直接取得予算 B*(δ; q, c)(r = 5, α = δ/D₀ = 0.05)。普遍下界(すべての c とすべての参照構造 L に対して成立)は c = 1 の達成可能上界とほぼ重なり、その差は (r−1)(1−α^{1/r}) < r 文書分である(命題3(ii))。c = 0 の線は全回収の停止時刻ではなく、仮定4の線形損失下での必要予算 B*(δ; q, 0) = q(1−α) を表す。
```

Rationale: 図中で下界線と c = 1 線が視覚的に重なるのは理論の内容そのもの(挟み撃ちの幅 < r)であり、読者が「線が欠けている」と誤読しないようキャプション水準で明示する。c = 0 線の定義も、旧版の比較量との取り違えを防ぐためキャプションに固定する。図の凡例・注記も同内容を表示するよう再生成済み。

---

## 2. A0関連

### 2.1 Location: §5.2 推定対象、A0 の三役 (i)–(iii) の直後

```diff
- (iii) LLM 観測者の基準方策からの乖離の定量化:D̂_I − D^seed_∞ として報告する。全適応方策上の最適性ギャップは、動的最適化の追加実装なしには主張しない。
+ (iii) LLM 観測者の基準方策からの乖離の定量化:D̂_I − D^seed_∞ として報告する。全適応方策上の最適性ギャップは、動的最適化の追加実装なしには主張しない。なお A0 は d = 1 についても D^seed_1 を計算する。固定された一つの参照グラフに対する A0 は、中核部分集合の全列挙により条件付き期待値を厳密に計算するものであり、一段追跡の閉形式(3)——参照グラフの生成まで平均した理論量——とは単一インスタンスでは一般に一致しない(厳密に一致するのは c = 0 と c = 1 のみ)。健全性検査 (i) での閉形式との照合は、独立生成インスタンス上の平均、または小規模設定での全参照グラフの厳密列挙(重み c^e(1−c)^{r(r−1)−e})によって行う。
```

Rationale: 固定グラフの条件付き期待値と、グラフ生成まで平均した理論量の混同を排除する。レプリケーションコードは r = 3 の全 64 参照グラフを厳密列挙し、重み付き平均が閉形式(3)と機械精度で一致すること(test_graph_averaged_d1_matches_closed_form_by_full_enumeration)、および単一の固定グラフでは一般に一致しないこと(test_single_fixed_graph_need_not_match_closed_form)の双方をテストで固定している。

### 2.2 Location: 付録C.2 観測者クラスの実装、末尾

```diff
+ なお、アームA0 は対話的観測者ではないため、環境 API を経由せず、生成器の内部メタデータ(中核集合と参照グラフ)から D^seed_d を厳密に計算する。同じ固定グラフ上で一括シード方策を API 経由で反復実行したときの期待値は、A0 の全列挙計算に一致する。ただしこの同値性は一括シード制限クラスに限られ、一般の逐次適応方策や LLM 方策には及ばない。API は LLM アーム(アームA)のための機構である。
```

Rationale: 実装の A0 は文書本文を読まず、メタデータから期待値を閉じた形で計算する。本文の API 記述(list/fetch/resolve)と実装の乖離に見える点を、同値性の理由つきで先回りして塞ぐ。

### 2.3 Location: 編集方針 A2、§4.1、§5.5 表1、§5.6 図2スロット

確認結果:整合。変更不要。コード・README・コメント・図・テストの全域で、A0 を D* のオラクルと呼ぶ表現は存在せず(禁止語句の不在はテスト `test_no_banned_phrases_in_code_or_docs` で機械的に検査)、呼称は「較正基準(calibration baseline)」「D^seed_∞」に統一されている。

---

## 3. 生成器関連

### 3.1 Location: §5.3 環境の生成、第一段落(生成手順の第二ステップ)

```diff
- 第二に、設計図を r 件の中核文書(議事録・答申・内部メモ等の類型)に符号化し、結合十分性と最小性を機械的に検査する。
+ 第二に、設計図を r 件の中核文書(議事録・答申・内部メモ等の類型)に符号化する。基準実装では Y₀ の成分数を K = r とし、各中核文書が Y₀ のちょうど一成分を排他的に担う一対一対応を採る——この規約が結合十分性・最小性と、仮定4の線形な回収—歪み写像を同時に、構成的に強制する。対応の成立は生成のたびに機械的に検査する。
```

Rationale: 実装は K = r の一対一対応で仮定4を設計時に強制しており(各中核文書は自成分の値を含み、他成分の値を含まないことを検査)、本文の「機械的に検査する」より強い保証を与えている。仮定4の明文化(v0.4 改訂 A3)と §5.3 を明示的に接続する。

### 3.2 Location: §5.3 環境の生成、第二段落(生成器の決定性)

```diff
- 生成器は決定的な骨格(設計図 → 文書計画 → テンプレート充填)を基本とし、LLM は表層の文体化にのみ用いる。
+ 生成器は決定的な骨格(設計図 → 文書計画 → テンプレート充填)を基本とし、基準実装は (q, r, c, seed) を与えれば完全に決定的で、LLM を一切用いない。LLM による表層の文体化は、生態学的妥当性を高めるための任意の追加層として主実験前に導入し、その場合のみ C.7 の開示を適用する。A0 による理論曲面の内部妥当性検査は、決定的生成のみで完結する。主稿で報告する観測者クラス別の達成歪み D̂_I(表1のアームA)には、別途 Blind-ID LLM アームの実行結果を要する。
```

Rationale: 現時点の実装はテンプレートのみで LLM 未使用。本文が「LLM を用いた」と読める余地を消し、AI 利用開示の適用条件を正確にする。A0 による内部妥当性検査が LLM 生成に依存しないことは、検証の頑健性としてむしろ利点である。

### 3.3 Location: §5.3 第二段落、漏洩検査の文

```diff
- 生成物は情報漏洩検査(撹乱が Y₀ 成分を含まないこと)を経る。
+ 生成物は情報漏洩検査を経る:撹乱文書が Y₀ の成分値を文字列として含まないこと、参照が中核文書間にのみ存在し撹乱文書が解決可能参照を持たないこと、中核文書が自成分以外の Y₀ 値を含まないこと(排他性)、文書 ID と表示順が中核・撹乱を識別させないこと(仮定2の実装的担保)を、インスタンスごとに機械検査し、違反時は生成を棄却する。
```

Rationale: 実装済みの検査項目(counts・排他性・漏洩・参照規律・禁止ラベル・ID/順序)を本文が過小に記述している。査読者が §5.3 だけ読んでも検査内容が分かるようにする。

### 3.4 Location: 付録C.3 採点、末尾

```diff
+ 採点鍵は生成器が Y₀ とともに出力する成分値そのものであり、コーパス生成と同時に決定的に確定する。
```

Rationale: 実装では y0 辞書が採点鍵として直接機能する。「事前確定した決定的採点鍵」の実体を一文で特定する。

### 3.5 Location: 末尾「レプリケーション資料」

```diff
- **レプリケーション資料**:合成コーパス生成器、全コーパスインスタンス(英日)、スクリプト版 π₀^∞(アームA0)、環境 API、解析コード、付録Bの再帰計算、本稿の全数値の検証スクリプトを、受理後に公開リポジトリに寄託する。
+ **レプリケーション資料**:リポジトリ transparency_sim(実装済み:合成コーパス生成器〔英語・決定的〕、A0 基準測定器〔d = 1 と d = ∞、部分集合全列挙による厳密計算〕、図1再生成スクリプト、理論式・生成器制約・A0 の端点性質と閉形式照合を検証する自動テスト)に、環境 API、LLM アーム、日本語並行コーパス、付録Bの再帰計算、全コーパスインスタンスを主実験までに追加し、受理後に公開する。
```

Rationale: 現在の実装状態と将来の追加分を分離し、「英日」等の未実装項目を既成事実のように読ませない。

---

## 4. Do not add yet

- Do not add Figure 2 results until the LLM Blind-ID arm has actually been run.
- Do not add any Metadata / Semantic-search observer results.
- Do not claim external validity or human-observer alignment in this manuscript yet.
- Do not report large A0 grids as "results"; A0 outputs so far are smoke-level sanity checks only (c = 0 and c = 1 identities verified exactly; monotone-in-q direction verified on one fixed graph).
- Do not describe the corpora as bilingual until the Japanese parallel generation is implemented.
